node_modules/
dist/
dist-ssr/
*.local
.env
.env.*
!.env.example

# Sorties de build PWA (régénérées à chaque build)
dev-dist/
public/sw.js.map
public/workbox-*.js

# Éditeurs
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Jamais dans le dépôt : secrets serveur, sauvegardes, archives
*.zip
secrets/
.diako-secrets/
supabase/.temp/

# Relevés de travail des scripts de photos : ils portent des chemins locaux
# (Desktop de la machine d'Andry) et se régénèrent en une commande. Le dépôt
# est PUBLIC — rien qui décrive l'arborescence d'un poste n'a à y figurer.
inventaire_photos.json
rapprochement.json
base_noms.json
auto.json
couvertures.json
photos_envoyees.json
scripts/__pycache__/
